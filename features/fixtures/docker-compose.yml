services:
  app:
    extra_hosts:
      host.docker.internal: host-gateway
    build:
      context: ../../
      dockerfile: ./features/fixtures/app/Dockerfile
      args:
       - GO_VERSION
    environment:
     - DEFAULT_MAZE_ADDRESS
    restart: "no"
    command: go run .
